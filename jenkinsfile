pipeline {
    agent {
        label 'master'
    }
  
    stages {
       stage('Checkout test') {
            steps {
              checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[]]])
           }
         }
         }
    }
